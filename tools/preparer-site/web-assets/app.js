// preparer-site placeholder script — real SPA lands in Phase 3.
(function () {
  var el = document.getElementById('app');
  if (el) el.textContent = 'preparer-site placeholder — Phase 3 will wire sql.js here.';
})();
